services:
  manxue-ai:
    build:
      context: .
      dockerfile: Dockerfile
    image: manxue-ai:local
    container_name: manxue-ai
    restart: unless-stopped
    ports:
      - "127.0.0.1:18765:8765"
    environment:
      ADMIN_TOKEN: ${ADMIN_TOKEN:-}
    volumes:
      - manxue-ai-data:/data
    mem_limit: 1g
    cpus: 1
    pids_limit: 256
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8765/api/auth/status', timeout=5)"]
      interval: 30s
      timeout: 10s
      start_period: 10s
      retries: 3

volumes:
  manxue-ai-data:
